# Cloudflare Workers 本地开发环境变量
# 复制此文件为 .dev.vars 并填入实际值
# .dev.vars 已被添加到 .gitignore，不会被提交到 Git

# NextAuth 配置
# 生产环境 URL，例如：https://your-app.your-subdomain.workers.dev
NEXTAUTH_URL=http://localhost:8787

# NextAuth 密钥（至少 32 字符）
# 可以使用以下命令生成：openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-key-here-min-32-characters-long

# 注意：D1 数据库通过 env.DB 访问，不需要连接字符串
# D1 数据库在 wrangler.toml 中配置，本地开发时自动创建
